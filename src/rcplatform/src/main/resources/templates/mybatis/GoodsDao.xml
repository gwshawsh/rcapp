<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruanchuangsoft.platform.dao.GoodsDao">


	<select id="queryObject" resultType="com.ruanchuangsoft.platform.entity.GoodsEntity">
		select * from goods where goods_id = #{value}
	</select>

	<select id="queryList" resultType="com.ruanchuangsoft.platform.entity.GoodsEntity">
		select * from goods 
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from goods 
	</select>
	 
	<insert id="save" parameterType="com.ruanchuangsoft.platform.entity.GoodsEntity" useGeneratedKeys="true" keyProperty="goodsId">
		insert into goods
		(
			`class_id`, 
			`name`, 
			`price`, 
			`gcount`, 
			`icon`, 
			`order_num`
		)
		values
		(
			#{classId}, 
			#{name}, 
			#{price}, 
			#{gcount}, 
			#{icon}, 
			#{orderNum}
		)
	</insert>
	 
	<update id="update" parameterType="com.ruanchuangsoft.platform.entity.GoodsEntity">
		update goods 
		<set>
			<if test="classId != null">`class_id` = #{classId}, </if>
			<if test="name != null">`name` = #{name}, </if>
			<if test="price != null">`price` = #{price}, </if>
			<if test="gcount != null">`gcount` = #{gcount}, </if>
			<if test="icon != null">`icon` = #{icon}, </if>
			<if test="orderNum != null">`order_num` = #{orderNum}</if>
		</set>
		where goods_id = #{goodsId}
	</update>
	
	<delete id="delete">
		delete from goods where goods_id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from goods where goods_id in 
		<foreach item="goodsId" collection="array" open="(" separator="," close=")">
			#{goodsId}
		</foreach>
	</delete>

</mapper>