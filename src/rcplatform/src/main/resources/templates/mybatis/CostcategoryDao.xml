<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruanchuangsoft.platform.dao.CostcategoryDao">

	<select id="queryObject" resultType="com.ruanchuangsoft.platform.entity.CostcategoryEntity">
		select * from costcategory where id = #{value}
	</select>

    <select id="queryListParentId" resultType="com.ruanchuangsoft.platform.entity.CostcategoryEntity">
        select * from costcategory where parent_id = #{parentId} order by order_num asc
    </select>

	<select id="queryList" resultType="com.ruanchuangsoft.platform.entity.CostcategoryEntity">
        select m.*,(select p.name from costcategory p where p.id = m.parent_id) as parentName
        from costcategory m
        <where>
            <if test="id!=null" >
                m.parent_id=#{id}
            </if>
        </where>
        order by m.order_num asc
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>

	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from costcategory 
	</select>
	 
	<insert id="save" parameterType="com.ruanchuangsoft.platform.entity.CostcategoryEntity" useGeneratedKeys="true" keyProperty="id">
		insert into costcategory
		(
			`parent_id`, 
			`code`, 
			`name`, 
			`remark`, 
			`status`, 
			`order_num`
		)
		values
		(
			#{parentId}, 
			#{code}, 
			#{name}, 
			#{remark}, 
			#{status}, 
			#{orderNum}
		)
	</insert>
	 
	<update id="update" parameterType="com.ruanchuangsoft.platform.entity.CostcategoryEntity">
		update costcategory 
		<set>
			<if test="parentId != null">`parent_id` = #{parentId}, </if>
			<if test="code != null">`code` = #{code}, </if>
			<if test="name != null">`name` = #{name}, </if>
			<if test="remark != null">`remark` = #{remark}, </if>
			<if test="status != null">`status` = #{status}, </if>
			<if test="orderNum != null">`order_num` = #{orderNum}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from costcategory where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from costcategory where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>